here are some templates I use for creating new Rails projects.

First template still in developmelt. Want to add some generators, so you can use all authentication and role features after using the template.